apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-be.fullname" . }}
  labels: {{- include "devops-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "devops-be.selectorLabels" . | nindent 4 }}
