apiVersion: v1
kind: Service
metadata:
  name: {{ include "crivo-fe.fullname" . }}
  labels:
    {{- include "crivo-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crivo-fe.selectorLabels" . | nindent 4 }}
