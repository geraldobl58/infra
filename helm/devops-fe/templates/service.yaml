apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-fe.fullname" . }}
  labels:
    {{- include "devops-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-fe.selectorLabels" . | nindent 4 }}
